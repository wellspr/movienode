import { LoginButton } from "@/app/[locale]/(auth)/components/LoginButton";

export default async function Page() {

    return (
        <div className="login">
            <LoginButton />
        </div>
    );
}